# Copy this file to `.env` and fill in real values.
# `.env` is gitignored and must never be committed.

# Hugging Face token — needed only to push the trained LoRA adapter to the Hub.
# Get one at https://huggingface.co/settings/tokens (free account).
HF_TOKEN=
